<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE configuration
        PUBLIC "-//mybatis.org//DTD Config 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-config.dtd">

<configuration>

    <!-- 日志 -->
    <settings>
        <setting name="logImpl" value="STDOUT_LOGGING"/>
    </settings>

    <!-- 别名 -->
    <typeAliases>
        <package name="com.bjpowernode.crm.settings.domain"/>
        <package name="com.bjpowernode.crm.workbench.domain"/>
    </typeAliases>

    <!-- mybatis-mapper 文件所在的包 -->
    <mappers>
        <package name="com.bjpowernode.crm.settings.mapper"/>
        <package name="com.bjpowernode.crm.workbench.mapper"/>
    </mappers>

</configuration>

<!--
    mybatis的主配置文件【作用】：
        1) 定义数据库的配置信息
        2) 指明 mapper 映射文件的位置
-->